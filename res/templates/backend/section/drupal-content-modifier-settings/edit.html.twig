{% extends "module.html.twig" %}

{% block section %}
    {% set config = {
        ready: true,
        mode: 'embedded',
        readonly: false,
        globalDocument: false,
        includes: false,
        debug: debug ?? false,
        uid: 'drupal-content-modifier-settings'
    } %}
    {{ include('configuration-editor/editor-setup.html.twig', {
        documentType: 'drupal-content-modifier-settings',
        returnUri: uri('page.core.index'),
        title: 'Content Modifier Settings',
        config,
        document
    }) }}
{% endblock %}